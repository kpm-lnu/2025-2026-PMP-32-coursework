# Drone Detection YOLOv8x — залежності Python
# Після: python -m venv .venv  &&  .\.venv\Scripts\Activate.ps1
#
# CPU (без NVIDIA GPU) — встановіть так:
#   pip install -r requirements.txt
#
# GPU (NVIDIA + CUDA) — спочатку PyTorch з CUDA, потім решту:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt

ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.23.0
matplotlib>=3.7.0
pillow>=9.0.0
pyyaml>=6.0
pandas>=1.5.0
scipy>=1.10.0
tqdm>=4.64.0
